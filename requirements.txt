# EEG-DDPM 项目依赖包
# Python 3.8 兼容版本

# 核心深度学习框架
torch>=1.12.0,<2.0.0
torchvision>=0.13.0,<0.15.0

# Diffusion 模型相关（兼容版本，避免 huggingface_hub 冲突）
diffusers==0.21.4
huggingface_hub>=0.16.0,<0.20.0
transformers>=4.21.0,<5.0.0
accelerate>=0.20.0

# 数据处理
numpy>=1.21.0,<1.25.0
scipy>=1.9.0
pandas>=1.5.0
scikit-learn>=1.1.0

# 图像处理和可视化
matplotlib>=3.5.0
Pillow>=9.0.0

# Excel 文件处理
openpyxl>=3.0.0

# 工具库
einops>=0.6.0
tqdm>=4.64.0
pywavelets>=1.3.0  # 小波变换（如果使用 CWT）

# 模型工具
torchsummary>=1.5.1

# 可选：TensorBoard（如果需要可视化）
# tensorboard>=2.10.0

